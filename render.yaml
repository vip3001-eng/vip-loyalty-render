services:
  - type: web
    name: vip-loyalty
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: FORCE_HTTPS_REDIRECT
        value: "0"
      - key: JWT_SECRET
        generateValue: true
